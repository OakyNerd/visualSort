#include "functions.hpp"

int main(){

    /*Generate a list of 100 random numbers in range 0-100*/

    /*Sorting algorithm runs as per the usual, we pass a reference to a vector of structs that the sorting algorithm adds to*/

    /*The struct will contain the "state" of the vector at a step in the sorting algorithm*/

    /*Let the user select the sort type by using a list of pointers to functions*/
    return 0;
}